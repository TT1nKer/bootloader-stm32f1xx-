# Bootloader 编译指南

## 前置要求

1. **CMake** (版本 >= 3.22)
   ```bash
   # macOS
   brew install cmake
   
   # Linux
   sudo apt-get install cmake
   
   # Windows
   # 方法1: 使用 Chocolatey
   choco install cmake
   
   # 方法2: 手动安装
   # 1. 从 https://cmake.org/download/ 下载 Windows 安装程序
   # 2. 运行安装程序，选择 "Add CMake to system PATH"
   # 3. 重启命令提示符或 PowerShell
   ```

2. **ARM GCC 工具链** (arm-none-eabi-gcc)
   ```bash
   # macOS
   brew install arm-none-eabi-gcc
   
   # Linux
   sudo apt-get install gcc-arm-none-eabi
   
   # Windows
   # 方法1: 使用 Chocolatey
   choco install gcc-arm-embedded
   
   # 方法2: 手动安装
   # 1. 从 https://developer.arm.com/downloads/-/gnu-rm 下载 Windows 版本
   # 2. 解压到 C:\Program Files\ARM 或类似目录
   # 3. 将 bin 目录添加到系统 PATH 环境变量
   #    例如: C:\Program Files\ARM\gcc-arm-none-eabi-xxx\bin
   ```

## 对于 Keil5 用户的快速开始指南（Windows）

> **重要说明**：本项目使用 **CMake + ARM GCC** 工具链，而不是 Keil5。如果您只熟悉 Keil5，请按照以下步骤操作。

### Keil5 vs CMake 的区别

| 特性 | Keil5 (MDK-ARM) | CMake + ARM GCC |
|------|----------------|-----------------|
| 编译方式 | 图形界面，点击按钮 | 命令行，输入命令 |
| 工具链 | ARM Compiler (ARMCC) | GCC (arm-none-eabi-gcc) |
| 项目文件 | `.uvprojx` 文件 | `CMakeLists.txt` 文件 |
| 输出文件 | `.axf`, `.hex` | `.elf`, `.bin`, `.hex` |
| 烧录文件 | 可以使用相同的 `.hex` 文件 | 可以使用相同的 `.hex` 文件 |

**好消息**：虽然编译方式不同，但生成的 `.hex` 文件可以直接用 Keil5 的烧录工具（ST-Link Utility）烧录！

### 快速开始（3 步完成编译）

#### 第 1 步：安装必要工具（只需一次）

打开 **命令提示符（CMD）** 或 **PowerShell**（以管理员身份运行），执行：

```cmd
REM 安装 Chocolatey（如果还没有安装）
REM 在 PowerShell 中运行：
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

REM 安装所有必需工具（一键安装）
choco install cmake gcc-arm-embedded ninja -y
```

**或者手动安装**（如果不想用 Chocolatey）：

1. **CMake**：
   - 访问 https://cmake.org/download/
   - 下载 Windows 安装程序
   - 安装时勾选 "Add CMake to system PATH"
   - 重启命令提示符

2. **ARM GCC**：
   - 访问 https://developer.arm.com/downloads/-/gnu-rm
   - 下载 Windows 版本（例如：`gcc-arm-none-eabi-10.3-2021.10-win32.exe`）
   - 安装到 `C:\Program Files\ARM\gcc-arm-none-eabi-xxx`
   - 将 `bin` 目录添加到系统 PATH：
     - 右键"此电脑" → "属性" → "高级系统设置" → "环境变量"
     - 在"系统变量"中找到 `Path`，点击"编辑"
     - 点击"新建"，添加：`C:\Program Files\ARM\gcc-arm-none-eabi-xxx\bin`
     - 确定保存，重启命令提示符

3. **Ninja**（可选，但推荐）：
   - 访问 https://github.com/ninja-build/ninja/releases
   - 下载 `ninja-win.zip`
   - 解压 `ninja.exe` 到 `C:\Windows\System32` 或添加到 PATH

#### 第 2 步：验证工具安装

打开新的命令提示符，输入以下命令检查：

```cmd
cmake --version
arm-none-eabi-gcc --version
ninja --version
```

如果都能显示版本号，说明安装成功！如果显示"不是内部或外部命令"，请检查 PATH 环境变量。

#### 第 3 步：编译项目（每次编译只需 3 条命令）

打开命令提示符，进入项目目录：

```cmd
REM 1. 进入项目目录（替换为您的实际路径）
cd C:\path\to\bootloaderfolder

REM 2. 创建构建目录并进入
mkdir build
cd build

REM 3. 配置 CMake（只需运行一次，或修改代码后重新运行）
cmake -G "Ninja" ..

REM 4. 编译（每次修改代码后运行）
ninja bootloader

REM 5. 检查大小（确保 < 4KB）
ninja check_size
```

**完成！** 编译好的文件在 `build\` 目录下：
- `bootloader.hex` - 用于烧录（与 Keil5 生成的格式相同）
- `bootloader.bin` - 二进制格式
- `bootloader.elf` - 用于调试

### 使用 Keil5 工具烧录（推荐方式）

虽然使用 CMake 编译，但您仍然可以使用熟悉的 Keil5 烧录工具：

#### 方法 1：使用 ST-Link Utility（最简单）

1. 打开 **ST-Link Utility**（Keil5 通常自带）
2. 连接 ST-Link 到电脑和开发板
3. 点击 **File** → **Open File**
4. 选择 `build\bootloader.hex` 文件
5. 点击 **Target** → **Program & Verify**
6. 点击 **Start** 开始烧录

#### 方法 2：使用 STM32CubeProgrammer

```cmd
REM 在命令提示符中运行
STM32_Programmer_CLI.exe -c port=SWD -w build\bootloader.hex -v -rst
```

### 常见问题解答

**Q: 为什么不能直接用 Keil5 打开这个项目？**  
A: 这个项目使用 CMake 构建系统，没有 `.uvprojx` 文件。CMake 是跨平台的现代构建工具，更适合团队协作和 CI/CD。

**Q: 生成的 `.hex` 文件和 Keil5 生成的一样吗？**  
A: 是的！都是标准的 Intel HEX 格式，可以用相同的工具烧录。

**Q: 我修改了代码，如何重新编译？**  
A: 只需在 `build` 目录下运行 `ninja bootloader` 即可，CMake 会自动检测哪些文件被修改。

**Q: 编译出错怎么办？**  
A: 查看错误信息，最常见的问题是：
- 工具未安装或 PATH 未配置 → 参考"第 1 步"
- 找不到源文件 → 确保在正确的目录下运行命令
- 链接错误 → 检查 `STM32F103XX_BOOTLOADER.ld` 文件是否存在

**Q: 可以像 Keil5 那样调试吗？**  
A: 可以！生成的 `bootloader.elf` 文件可以用：
- **OpenOCD + GDB**（命令行调试）
- **VS Code + Cortex-Debug 插件**（图形界面调试，类似 Keil5）
- **Keil5 的调试器**（如果支持加载 `.elf` 文件）

### 完整工作流程示例

```cmd
REM ============================================
REM 第一次使用（只需一次）
REM ============================================
REM 1. 安装工具
choco install cmake gcc-arm-embedded ninja -y

REM 2. 进入项目目录
cd C:\Users\YourName\bootloaderfolder

REM 3. 创建构建目录
mkdir build
cd build

REM 4. 配置项目
cmake -G "Ninja" ..

REM ============================================
REM 日常开发（每次修改代码后）
REM ============================================
REM 1. 进入构建目录
cd C:\Users\YourName\bootloaderfolder\build

REM 2. 编译
ninja bootloader

REM 3. 检查大小
ninja check_size

REM 4. 查看输出文件
dir bootloader.*

REM 5. 使用 ST-Link Utility 烧录 bootloader.hex
```

### 下一步

- 熟悉后可以尝试使用 **VS Code + CMake 插件**，获得类似 Keil5 的集成开发体验
- 了解更多 CMake 命令，参考下面的"编译步骤"章节
- 如果遇到问题，参考"常见问题"章节

---

## 编译步骤

### 1. 创建构建目录

**macOS/Linux:**
```bash
cd ~/bootloaderfolder
mkdir -p build
cd build
```

**Windows (PowerShell):**
```powershell
cd C:\path\to\bootloaderfolder
mkdir build
cd build
```

**Windows (CMD):**
```cmd
cd C:\path\to\bootloaderfolder
mkdir build
cd build
```

### 2. 配置 CMake

**macOS/Linux:**
```bash
cmake ..
```

**Windows (使用 MinGW Makefiles):**
```cmd
cmake -G "MinGW Makefiles" ..
```

**Windows (使用 Visual Studio):**
```cmd
cmake -G "Visual Studio 17 2022" ..
```

**Windows (使用 Ninja，推荐):**
```cmd
cmake -G "Ninja" ..
```

### 3. 编译

**macOS/Linux:**
```bash
make bootloader
```

或者使用多线程编译（更快）：
```bash
make -j4 bootloader
```

**Windows (MinGW Makefiles):**
```cmd
mingw32-make bootloader
```

或者多线程编译：
```cmd
mingw32-make -j4 bootloader
```

**Windows (Visual Studio):**
```cmd
cmake --build . --config Release --target bootloader
```

**Windows (Ninja，推荐):**
```cmd
ninja bootloader
```

或者多线程编译：
```cmd
ninja -j4 bootloader
```

### 4. 检查大小

**macOS/Linux:**
```bash
# 使用 CMake 目标
make check_size

# 或使用脚本
../check_bootloader_size.sh
```

**Windows (MinGW Makefiles):**
```cmd
mingw32-make check_size
```

**Windows (Visual Studio):**
```cmd
cmake --build . --config Release --target check_size
```

**Windows (Ninja):**
```cmd
ninja check_size
```

**Windows (使用脚本):**
```powershell
# PowerShell
..\check_bootloader_size.sh

# 或使用 Git Bash
bash ../check_bootloader_size.sh
```

### 5. 查看输出文件

编译完成后，在 `build/` 目录下会生成：
- `bootloader.elf` - ELF 格式（用于调试）
- `bootloader.bin` - 二进制格式（用于烧录）
- `bootloader.hex` - Intel HEX 格式（用于烧录）
- `bootloader.map` - 链接映射文件

## 烧录

### 使用 st-flash

**macOS/Linux:**
```bash
# 烧录二进制文件
st-flash write build/bootloader.bin 0x08000000

# 或烧录 HEX 文件
st-flash write build/bootloader.hex 0x08000000
```

**Windows:**
```cmd
# 安装 st-flash (使用 Chocolatey)
choco install stlink

# 或从 https://github.com/stlink-org/stlink/releases 下载 Windows 版本

# 烧录二进制文件
st-flash.exe write build\bootloader.bin 0x08000000

# 或烧录 HEX 文件
st-flash.exe write build\bootloader.hex 0x08000000
```

### 使用 OpenOCD

**macOS/Linux:**
```bash
openocd -f interface/stlink.cfg -f target/stm32f1x.cfg \
    -c "program build/bootloader.elf verify reset exit"
```

**Windows:**
```cmd
# 安装 OpenOCD (使用 Chocolatey)
choco install openocd

# 或从 https://github.com/openocd-org/openocd/releases 下载 Windows 版本

# 烧录
openocd.exe -f interface/stlink.cfg -f target/stm32f1x.cfg ^
    -c "program build\bootloader.elf verify reset exit"
```

### 使用 STM32CubeProgrammer (Windows/macOS/Linux)

**Windows:**
```cmd
# 下载并安装 STM32CubeProgrammer
# 从 https://www.st.com/en/development-tools/stm32cubeprog.html 下载

# 使用命令行烧录
STM32_Programmer_CLI.exe -c port=SWD -w build\bootloader.hex -v -rst
```

**macOS/Linux:**
```bash
# 下载并安装 STM32CubeProgrammer
# 从 https://www.st.com/en/development-tools/stm32cubeprog.html 下载

# 使用命令行烧录
STM32_Programmer_CLI -c port=SWD -w build/bootloader.hex -v -rst
```

### 使用 ST-Link Utility (Windows)

```cmd
# 下载并安装 ST-Link Utility
# 从 https://www.st.com/en/development-tools/stsw-link004.html 下载

# 使用图形界面：
# 1. 打开 ST-Link Utility
# 2. File -> Open File -> 选择 bootloader.hex
# 3. Target -> Program & Verify
# 4. 点击 "Start" 开始烧录
```

## 完整编译流程

### macOS/Linux

```bash
# 1. 进入项目目录
cd ~/bootloaderfolder

# 2. 创建并进入构建目录
mkdir -p build && cd build

# 3. 配置 CMake
cmake ..

# 4. 编译
make bootloader

# 5. 检查大小（必须 < 4KB）
make check_size

# 6. 查看输出
ls -lh bootloader.*

# 7. 烧录（可选）
st-flash write bootloader.bin 0x08000000
```

### Windows (使用 Ninja，推荐)

```cmd
REM 1. 进入项目目录
cd C:\path\to\bootloaderfolder

REM 2. 创建并进入构建目录
mkdir build
cd build

REM 3. 配置 CMake (使用 Ninja)
cmake -G "Ninja" ..

REM 4. 编译
ninja bootloader

REM 5. 检查大小（必须 < 4KB）
ninja check_size

REM 6. 查看输出
dir bootloader.*

REM 7. 烧录（可选）
st-flash.exe write bootloader.bin 0x08000000
```

### Windows (使用 MinGW Makefiles)

```cmd
REM 1. 进入项目目录
cd C:\path\to\bootloaderfolder

REM 2. 创建并进入构建目录
mkdir build
cd build

REM 3. 配置 CMake
cmake -G "MinGW Makefiles" ..

REM 4. 编译
mingw32-make bootloader

REM 5. 检查大小（必须 < 4KB）
mingw32-make check_size

REM 6. 查看输出
dir bootloader.*

REM 7. 烧录（可选）
st-flash.exe write bootloader.bin 0x08000000
```

### Windows (使用 Visual Studio)

```cmd
REM 1. 进入项目目录
cd C:\path\to\bootloaderfolder

REM 2. 创建并进入构建目录
mkdir build
cd build

REM 3. 配置 CMake
cmake -G "Visual Studio 17 2022" ..

REM 4. 编译
cmake --build . --config Release --target bootloader

REM 5. 检查大小（必须 < 4KB）
cmake --build . --config Release --target check_size

REM 6. 查看输出
dir bootloader.*

REM 7. 烧录（可选）
st-flash.exe write bootloader.bin 0x08000000
```

## 清理构建

**macOS/Linux:**
```bash
# 清理构建文件
cd build
make clean

# 或删除整个构建目录
cd ..
rm -rf build
```

**Windows (MinGW Makefiles):**
```cmd
cd build
mingw32-make clean

REM 或删除整个构建目录
cd ..
rmdir /s /q build
```

**Windows (Visual Studio):**
```cmd
cd build
cmake --build . --config Release --target clean

REM 或删除整个构建目录
cd ..
rmdir /s /q build
```

**Windows (Ninja):**
```cmd
cd build
ninja clean

REM 或删除整个构建目录
cd ..
rmdir /s /q build
```

**Windows (PowerShell):**
```powershell
cd build
ninja clean

# 或删除整个构建目录
cd ..
Remove-Item -Recurse -Force build
```

## 调试编译

如果编译失败，可以查看详细输出：

**macOS/Linux:**
```bash
make VERBOSE=1 bootloader
```

**Windows (MinGW Makefiles):**
```cmd
mingw32-make VERBOSE=1 bootloader
```

**Windows (Visual Studio):**
```cmd
cmake --build . --config Release --target bootloader --verbose
```

**Windows (Ninja):**
```cmd
ninja -v bootloader
```

## 常见问题

### 1. 找不到 arm-none-eabi-gcc

**错误**：`arm-none-eabi-gcc: command not found` 或 `'arm-none-eabi-gcc' 不是内部或外部命令`

**解决 (macOS/Linux):**
```bash
# 检查是否安装
which arm-none-eabi-gcc

# 如果未安装，安装工具链
brew install arm-none-eabi-gcc  # macOS
# 或
sudo apt-get install gcc-arm-none-eabi  # Linux
```

**解决 (Windows):**
```cmd
REM 检查是否安装
where arm-none-eabi-gcc

REM 如果未安装，使用 Chocolatey 安装
choco install gcc-arm-embedded

REM 或手动安装：
REM 1. 从 https://developer.arm.com/downloads/-/gnu-rm 下载
REM 2. 解压到 C:\Program Files\ARM\gcc-arm-none-eabi-xxx
REM 3. 将 bin 目录添加到系统 PATH
REM    例如: C:\Program Files\ARM\gcc-arm-none-eabi-xxx\bin
REM 4. 重启命令提示符或 PowerShell
```

### 2. CMake 版本过低

**错误**：`CMake 3.22 or higher is required`

**解决 (macOS/Linux):**
```bash
# 升级 CMake
brew upgrade cmake  # macOS
# 或
sudo apt-get update && sudo apt-get upgrade cmake  # Linux
```

**解决 (Windows):**
```cmd
REM 使用 Chocolatey 升级
choco upgrade cmake

REM 或手动安装：
REM 1. 从 https://cmake.org/download/ 下载最新版本
REM 2. 运行安装程序，选择 "Add CMake to system PATH"
REM 3. 重启命令提示符或 PowerShell
```

### 2.1. Windows 找不到 make 命令

**错误**：`'make' 不是内部或外部命令`

**解决：**
```cmd
REM 方法1: 安装 MinGW (推荐)
choco install mingw

REM 方法2: 使用 Ninja (更推荐)
choco install ninja
cmake -G "Ninja" ..

REM 方法3: 使用 Visual Studio
cmake -G "Visual Studio 17 2022" ..
cmake --build . --config Release
```

### 3. 链接脚本找不到

**错误**：`cannot open linker script file`

**解决**：确保 `STM32F103XX_BOOTLOADER.ld` 在项目根目录

### 4. Bootloader 超过 4KB

**错误**：链接时显示 `region 'FLASH' overflowed`

**解决**：
- 启用编译器优化（已在 CMakeLists.txt 中配置 `-Os`）
- 检查是否有未使用的代码
- 移除调试代码（printf 等）

### 5. Windows PATH 环境变量问题

**错误**：命令找不到，即使已安装工具

**解决：**
```cmd
REM 1. 检查 PATH 是否包含工具链目录
echo %PATH%

REM 2. 临时添加到 PATH（当前会话）
set PATH=%PATH%;C:\Program Files\ARM\gcc-arm-none-eabi-xxx\bin

REM 3. 永久添加到 PATH：
REM    - 右键 "此电脑" -> "属性" -> "高级系统设置"
REM    - 点击 "环境变量"
REM    - 在 "系统变量" 中找到 "Path"，点击 "编辑"
REM    - 点击 "新建"，添加工具链的 bin 目录
REM    - 点击 "确定" 保存
REM    - 重启命令提示符或 PowerShell
```

### 6. Windows 权限问题

**错误**：访问被拒绝或需要管理员权限

**解决：**
```cmd
REM 以管理员身份运行命令提示符或 PowerShell
REM 右键点击 -> "以管理员身份运行"

REM 或使用 Chocolatey 安装工具（通常不需要管理员权限）
choco install cmake gcc-arm-embedded ninja
```

## 编译选项

### Debug 模式

**macOS/Linux:**
```bash
cd build
cmake -DCMAKE_BUILD_TYPE=Debug ..
make bootloader
```

**Windows (MinGW Makefiles):**
```cmd
cd build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
mingw32-make bootloader
```

**Windows (Ninja):**
```cmd
cd build
cmake -G "Ninja" -DCMAKE_BUILD_TYPE=Debug ..
ninja bootloader
```

**Windows (Visual Studio):**
```cmd
cd build
cmake -G "Visual Studio 17 2022" ..
cmake --build . --config Debug --target bootloader
```

### Release 模式（默认，优化大小）

**macOS/Linux:**
```bash
cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
make bootloader
```

**Windows (MinGW Makefiles):**
```cmd
cd build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release ..
mingw32-make bootloader
```

**Windows (Ninja):**
```cmd
cd build
cmake -G "Ninja" -DCMAKE_BUILD_TYPE=Release ..
ninja bootloader
```

**Windows (Visual Studio):**
```cmd
cd build
cmake -G "Visual Studio 17 2022" ..
cmake --build . --config Release --target bootloader
```

## 验证编译结果

编译成功后，应该看到：

```
   text    data     bss     dec     hex filename
   xxxx     xxx      xx    xxxx    xxxx bootloader.elf
```

**检查标准**：`text + data < 4096` (4KB)

## 自动化脚本

可以使用提供的脚本自动检查大小：

**macOS/Linux:**
```bash
./check_bootloader_size.sh
```

**Windows (Git Bash 或 WSL):**
```bash
bash check_bootloader_size.sh
```

**Windows (PowerShell):**
```powershell
# 如果安装了 Git Bash
bash check_bootloader_size.sh

# 或使用 WSL
wsl bash check_bootloader_size.sh
```

脚本会：
- 检查 bootloader.elf 是否存在
- 显示详细的大小信息
- 检查是否超过 4KB 限制
- 如果接近限制会给出警告

## Windows 特定工具推荐

### 推荐的 Windows 开发环境设置

1. **包管理器 (Chocolatey)**
   ```cmd
   REM 安装 Chocolatey (以管理员身份运行 PowerShell)
   Set-ExecutionPolicy Bypass -Scope Process -Force
   [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
   iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
   ```

2. **一键安装所有工具**
   ```cmd
   choco install cmake gcc-arm-embedded ninja stlink openocd -y
   ```

3. **推荐的构建工具**
   - **Ninja** (最快，推荐)
   - **MinGW Makefiles** (兼容性好)
   - **Visual Studio** (适合 Visual Studio 用户)

4. **推荐的烧录工具**
   - **STM32CubeProgrammer** (官方工具，功能全面)
   - **st-flash** (命令行工具，简单快速)
   - **ST-Link Utility** (图形界面，适合初学者)

